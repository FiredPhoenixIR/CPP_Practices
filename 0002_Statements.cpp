#include<iostream>
using namespace std;
/*
int main() {
	int i;
	i = 6 * 4; 
	// i LVALUE , a memory location   
	// All variables are LVALUE
	// RVALUE is temporary and nothing assigned to it
	// Literals like 24 are RVALUE -- exists in memory only once
	//6 * 8 = i; // Compile error lvalue should be left of assignment
	int a;
	a = 15, 5;
	int b = (15, 5);
	auto c = "salam";
	//auto c = 2;
	cout << c << endl << a << endl << b << endl;

	// Specify type so compiler know how much memory it should allocate for it
}
*/