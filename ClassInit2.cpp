#include <iostream>
using namespace std;
/*
class AreaOfTriangle {
public:
    AreaOfTriangle() = default;
    // AreaOfTriangle() { length = 0; height = 0; }
    // Constructor initializer list to initialize length and height
    AreaOfTriangle(float l, float h) : length(l), height(h) {}
    float get_Area() const { return (length * height / 2); }
    void setHeight(float h) {
        height = h;
    }
    void setLength(float l) {
        length = l;
    }
private: // protected
    float length = 0;
    float height = 0;
};

int main() {

    // make the class object here 
    AreaOfTriangle obj;
    //set the length and height of the object here 
    obj.setHeight(5.5);
    obj.setLength(7.5);
    float answer = obj.get_Area(); //stores value of the object area

    cout << "Area: " << answer << endl; //call area function for the object here

    return 0;
}
*/