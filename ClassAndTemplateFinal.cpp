#include <iostream>
using namespace std;
/*
// Define the class Complex here 
template<class T>
class Complex {
private:
    T real;
    T imaginary;

public:
    Complex(T a, T b);
    T get_real();
    T get_im();
};

// Define the constructor "Complex" here 
template<class T>
Complex<T>::Complex(T a, T b) {
    real = a;
    imaginary = b;
}

// Define function get_im()
template<class T>
T Complex<T>::get_real() {
    return real;
}

template<class T>
T Complex<T>::get_im() {
    return imaginary;
}

int main() {
    double x = 500, y = 100;
    // Uncomment line below to initialize the object
    Complex<double> comp(x, y);

    // Uncomment the lines below to test both the functions and display the result
    cout << "The complex number is: " << x << "+ " << y << "i" << endl;
    cout << "Real part of the complex number is: " << comp.get_real() << endl;
    cout << "Imaginary part of the complex number is: " << comp.get_im() << endl;
}
*/