#include <iostream>
using namespace std;
/*
void withdraw(double* balance, double amount) {
    if (amount > *balance) {
        cout << "You are broke. You don’t have enough money. Go away" << endl;
    }
    else {
        *balance -= amount;
    }

}

void deposit(double* balance, double amount) {
    *balance += amount;
}

int main() {
    double blnc = 1000;
    deposit(&blnc, 199);
    withdraw(&blnc, 1200);
    cout << "Balance is : " << blnc;

}
*/