#include<iostream>
using namespace std;
/*
static int fib(int n) {
	if (n == 0)
		return 0;
	if (n == 1 || n == 2)
		return 1;
	else
		return (fib(n - 1) + fib(n - 2));
}

int main() {
	int x;
	cout << "Declare you number :" << endl;
	cin >> x;
	cout << x << " of fibonachi is " << fib(x);
}
*/