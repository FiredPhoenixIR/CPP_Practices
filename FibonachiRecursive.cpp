#include<iostream>
using namespace std;
/*
static int fib(int n) {
	if (n == 0)
		return 0;
	if (n == 1 || n == 2)
		return 1;
	else
		return (fib(n - 1) + fib(n - 2));
}

int main() {
	int x;
	cout << "Declare you number :" << endl;
	cin >> x;
	cout << x << " of fibonachi is " << fib(x);
}

void test(int range)
{
    int first = 0, second = 1, fibonacci = 0;
    for ( int c = 0 ; c < range ; c++ )
    {
        if ( c <= 1 ){
            fibonacci = c;
        }else
        {
            fibonacci = first + second;
            first = second;
            second = fibonacci;
        }
        cout << fibonacci <<" ";
    }
}
*/

/*
#include <iostream>
using namespace std;


int fibonacci(int n)
{
    if (n==0)
      return 0;
    if (n==1 || n==2)
      return 1;
    else
      return (fibonacci(n-1)+fibonacci(n-2));
}

void test (int n ){
  for (int i = 0 ; i < n ; i++){
    cout << fibonacci(i) << " ";
  }
}

*/