#include <iostream>
#include <vector>

/*
#include <iostream>
#include <vector>
#include <list>

int main() {
    std::vector<int> vec = {1, 2, 3, 4, 5};
    std::list<int> lst = {1, 2, 3, 4, 5};

    std::cout << "Vector:";
    for (int num : vec)
        std::cout << " " << num;
    std::cout << std::endl;

    std::cout << "List:";
    for (int num : lst)
        std::cout << " " << num;
    std::cout << std::endl;

    return 0;
}

*/

//Iterate
/*
int main() {
    std::vector<int> vec = { 1, 2, 3, 4, 5 };

    // Using iterators to print vector elements
    for (std::vector<int>::iterator it = vec.begin(); it != vec.end(); ++it)
        std::cout << *it << " ";
    std::cout << std::endl;

    return 0;
}
*/

//Sort Algorithm
/*
#include <iostream>
#include <algorithm>
#include <vector>

int main() {
    std::vector<int> vec = {5, 3, 2, 4, 1};

    // Sorting the vector
    std::sort(vec.begin(), vec.end());

    // Printing the sorted vector
    for (int num : vec)
        std::cout << num << " ";
    std::cout << std::endl;

    return 0;
}
*/